# parameter_introduction.py

names = ['Alice', 'Bob', 'Charlie', 'Donna']

for name in names:
    print (f"{name}", end=" -> ")


print (names[0], names[1], names[2], names[3], sep=", ") 


v = "99"

val = int(v)
r = range()